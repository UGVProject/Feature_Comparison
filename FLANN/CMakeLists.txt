CMAKE_MINIMUM_REQUIRED( VERSION 2.8 )
PROJECT( FLANNTest )

#SET(CMAKE_CXX_COMPILER "g++")
#SET( CMAKE_BUILD_TYPE Debug  )

# 增加opencv的依赖
FIND_PACKAGE( OpenCV REQUIRED )

ADD_EXECUTABLE( flann 91_FlannBasedMatcher.cpp )
TARGET_LINK_LIBRARIES( flann ${OpenCV_LIBS})
